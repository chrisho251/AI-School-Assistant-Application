"""Hybrid retrieval + RRF + reranker. Phase 4 (D25-D30)."""

from __future__ import annotations

# TODO D25: dense_search(query, notebook_id, k=30)
# TODO D26: sparse_search(query, notebook_id, k=30)
# TODO D27: reciprocal_rank_fusion(*ranked_lists, k=60) -> fused
# TODO D29: retrieve(query, notebook_id, k_final=5) — full pipeline
