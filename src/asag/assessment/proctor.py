"""Browser-side lockdown helpers — record events sent from FE. Phase 11 (D65)."""

from __future__ import annotations

# TODO D65: log_proctor_event(attempt_id, event_type, payload)
# TODO D65: policy.evaluate(events) -> {action: warn|auto_submit|flag}
